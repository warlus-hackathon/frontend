# frontend

# install
```bash
git clone https://github.com/warlus-hackathon/frontend.git 
cd frontend
```

# install dependencies

## install poetry:

```bash
pip install poetry 
```
## install of packages:

```bash
poetry install
```

## set enviroment

create .env file (see .env.default)

## usage:

```bash
make run
```